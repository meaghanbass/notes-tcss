import React from "react"

export default ({ children }) => {
    return (
        <React.Fragment>

            <h3 className="text-gray-700 font-bold">{children}</h3>

        </React.Fragment>
    )
}

